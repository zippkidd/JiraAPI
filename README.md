Remember that Zapier CLI is using node version 14.

I recommend using [NVM (node version manager)](https://github.com/nvm-sh/nvm) to ensure you're running the same version of node that Zapier will be using.

